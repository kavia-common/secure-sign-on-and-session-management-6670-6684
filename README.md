# secure-sign-on-and-session-management-6670-6684

This workspace contains the `auth_backend` Spring Boot service that implements OAuth2/SSO login flows (Google, GitHub, Microsoft), JWT-based sessions, and REST endpoints for login, callback, logout, and session validation. See `auth_backend/README.md` for details.